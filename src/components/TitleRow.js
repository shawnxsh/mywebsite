import React from "react";

function TitleRow({ title }) {
  return (
    <div className="self-start px-8 font-Caveat text-[2.5em] font-semibold">
      {title}
    </div>
  );
}

export default TitleRow;
